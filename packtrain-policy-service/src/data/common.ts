export enum ExtensionStatus {
    APPLIED = "applied",
    IGNORED = "ignored",
    APPROVED = "approved",
    REJECTED = "rejected",
    PENDING = "pending",
    NO_EXTENSION = "no_extension",
}

export enum SubmissionStatus {
    MISSING = "missing",
    EXCUSED = "excused",
    LATE = "late",
    EXTENDED = "extended",
    ON_TIME = "on_time",
}
