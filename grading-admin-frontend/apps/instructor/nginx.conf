server {
    listen 80;
    server_name localhost;

    root /usr/share/nginx/html;
    index index.html;

    location /health {
        return 200 'OK';
    }

    location /instructor/ {
        try_files $uri $uri/ /instructor/index.html;
    }

    location /instructor/assets/ {
        try_files $uri =404;
        access_log off;
        expires 1y;
        add_header Cache-Control "public, max-age=31536000, immutable";
    }

    error_page 404 /instructor/index.html;
}
