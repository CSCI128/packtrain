spring:
  application:
    name: Grading Admin Service - Test
  web:
    resources:
      add-mappings: false
  jpa:
    hibernate:
      ddl-auto: create-drop
    open-in-view: false

server:
  error:
    whitelabel:
      enabled: false
