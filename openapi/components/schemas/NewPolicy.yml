description: Create a new policy file
type: object
required:
  - name
  - file_path
  - file_data
properties:
  name:
    type: string
    example: Default Policy
  file_path:
    type: string
    example: default.js
  file_data:
    type: string
    format: binary
    example: |
      // valid javascript code
